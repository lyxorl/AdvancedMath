#include "AdvancedMath.h"
#include <stdio.h>

int main(){

    // //first test to print complex number
    // complexNumber test;
    // test.real = 1.2;
    // test.imaginary = 1.2;
    // printComplexNumber(test, true, 1, 0);
    // printf("\n");
    // printComplexNumber(test, false, 2, 0);
    // printf("\n");
    // Check

    //first test polynomial
    //polynomial polytest;
    //polytest = createPolynom(1);
    //Obj coeff0;
    //coeff0.type = FLOAT;
    //*(float *)(coeff0.value) = 1.5321;
    //polytest.value[0] = coeff0;
    //polytest.value[1] = coeff0;
    //printPolynomial(polytest, 'X', 1);
    //Check
    

    return 0;
}